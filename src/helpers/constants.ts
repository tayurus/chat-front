export const DATE_TIME_FORMAT = "YYYY-MM-DD HH:mm:ss";
export const DEFAULT_EMPTY_STRING = "";

export enum MESSAGE_TYPE {
  OUTGOING = "outgoing",
  INCOMING = "incoming",
}

export const NEW_DIALOG_ID = "new";

export const REMOVE_DIALOG_TYPER_TIMEOUT = 1000;
